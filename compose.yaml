services:
  localscribe:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: localscribe
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./config:/app/config:ro
    command: ["python", "-m", "app.main"]

  bootstrap-models:
    build:
      context: .
      dockerfile: Dockerfile
    profiles: ["setup"]
    env_file:
      - .env
    volumes:
      - ./models:/app/models
    command: ["python", "scripts/bootstrap_models.py"]
